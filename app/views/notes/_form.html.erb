<%= form_for(note) do |f| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :note %>
    <%= f.text_area :note %>
  </div>

  <div class="field">
    <%= f.label :constituent_id %>
    <%= f.number_field :constituent_id %>
  </div>

  <div class="field">
    <%= f.label :cca %>
    <%= f.text_field :cca %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
